# Production: leave VITE_API_BASE_URL empty so the Vercel rewrites in vercel.json
# proxy /api/* to the backend. Only set this if you are NOT using Vercel rewrites.
# VITE_API_BASE_URL=https://your-api-sentinel-backend.onrender.com
VITE_API_BASE_URL=
